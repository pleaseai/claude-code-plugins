{
  "description": "Rewrites code.claude.com/docs page URLs to their .md source so WebFetch gets clean markdown instead of the HTML app shell",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "WebFetch",
        "hooks": [
          {
            "type": "command",
            "command": "bash ./hooks/rewrite-docs-url.sh",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
