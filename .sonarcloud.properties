# Vendored upstream code is read-only in this repo (fixes go upstream),
# so exclude it from SonarCloud automatic analysis.
sonar.exclusions=plugins/skillopt-sleep/skillopt_sleep/**,external-plugins/**
